// ─────────────────────────────────────────────────────────────
//  Screen Recorder — Branding Config
//  Edit the values below. Reload the extension when done.
// ─────────────────────────────────────────────────────────────

window.SR_BRANDING = {

  // ── Identity ───────────────────────────────────────────────
  name:    '',          // e.g. 'Sarah Johnson'
  title:   '',        // e.g. 'Product Designer'
  company: '',       // e.g. 'Acme Inc.' — leave '' to hide
  website: '',    // e.g. 'sarah.design'  — leave '' to hide

  // ── Appearance ─────────────────────────────────────────────
  show:         true,            // set false to disable branding entirely
  accentColor:  '#ff3b30',       // left bar colour — your brand colour
  position:     'bottom-right',  // 'bottom-left' | 'bottom-right'

  // ── Layout Template ────────────────────────────────────────
  template: 'side-by-side',     // 'side-by-side' | 'screen-focus'

};
